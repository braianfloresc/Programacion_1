#PROGRAMACION 1 
"""
Reglas obligatorias

    No debe contener acentos, ñ, o ç
    No debe empezar por un número o símbolo (excepto guión bajo _)
    No puede ser una palabra reservada del lenguaje

Convenciones recomendadas

    Usar snake_case: todo en minúsculas con guiones bajos separando palabras
    Elegir nombres descriptivos que indiquen qué contiene la variable
    Mantener consistencia en el estilo de nombrado
"""
#PALABRAS RESERVADAS DE PYHTON
"""
    False   None    True     and    as  assert  async
    await   break   class   continue    def     del    elif
    else    except  finally     for     from    global  if  
    import  in      is      lambda      nonlocal    not     or
    pass    raise   return   try        while       whit    yield

"""
#TIPOS DE DATOS
"""
STRING (cadena de texto)
    Los strings son un tipo de dato que utilizamos para almacenar cadenas de caracteres. Se definen encerrando el texto entre comillas simples (' ') o dobles (" ").
        longitud: La función len() devuelve el número de caracteres

INT (integers)
    Son el tipo de dato que representa los valores numéricos enteros, sin parte decimal.

FLOAT 
    Sirven para representar los valores con números decimales. Se escriben con un punto como separador decimal.

BOOL (booleano)
    Es un tipo de dato que solo puede recibir dos valores: verdadero (True) o falso (False).

"""
#FUNCIONES DE CONVERSION
"""
    str(): Convierte a cadena de caracteres
    int(): Convierte a número entero
    float(): Convierte a número decimal
    complex(): Convierte a número complejo

"""
#OPERADORES

"""
    Aritméticos: Para operaciones matemáticas básicas (+, -, *, /, etc.)
    Relacionales: Para comparar valores (==, !=, >, <, etc.)
    Lógicos: Para operaciones de lógica booleana (and, or, not)
    Asignación: Para asignar valores a variables (=, +=, -=, etc.)
    Identidad: Para comprobar si dos objetos son el mismo (is, is not)

"""
#OPERADORES DE COMPARACIÓN Y LÓGICOS
"""
Operadores de comparación
    ==: Igualdad
    !=: Desigualdad
    >: Mayor que
    <: Menor que
    >=: Mayor o igual que
    <=: Menor o igual que

Operadores lógicos
    and: Y lógico (ambas condiciones deben ser True)
    or: O lógico (al menos una condición debe ser True)
    not: Negación lógica (invierte el valor de verdad)


"""
print ("Hola Mundo")